package com.car.mp3player.model

enum class PlaybackMode {
    ORDER,
    SHUFFLE
}

enum class ThemeMode {
    LIGHT,
    DARK,
    SYSTEM
}

enum class LyricFilter {
    ALL,
    WITH_LRC
}
