# MP3播放器

比亚迪 DiLink 车机本地音乐播放器，支持 **卡拉 OK 逐字歌词**、**全屏封面播放页**、**桌面/仪表屏悬浮歌词**、**方向盘 / 语音切歌**。

当前版本：**v3.2.0**

## 功能一览

### 播放与列表
- 扫描 SD 卡 / 车机 `Music` 目录 / 自选文件夹中的 MP3 等音频
- **播放列表缓存**：扫描结果自动保存，再次打开立即显示，无需等待重新扫描
- 底部三页导航：播放列表 · 播放 · 设置
- 搜索、按歌手浏览、按歌名/时长排序
- 点进歌手后只播放该歌手的歌曲
- 顺序播放 / 随机播放
- 进度条拖动、当前时长 / 总时长显示
- **开机自动启动并续播**（后台恢复播放并打开 App，需开启设置）
- **启动时自动恢复播放**

### 歌词
- 自动匹配同名 `.lrc` 文件；无本地歌词时自动联网获取
- **多源歌词搜索**：依次尝试 LrcLib → 网易云 → 酷狗 → QQ 音乐 → Lyrics.ovh，一个搜不到自动换下一个
- **长按播放页歌词**可修改歌名/歌手并重新搜索
- 应用内滚动歌词 + 全局悬浮双行歌词 + **仪表屏滚动歌词**（需车机有副屏）
- 卡拉 OK：已唱字变色，未唱字半透明
- 歌词过长自动换行（最多 1–4 行），不超出屏幕
- **歌词配色**（字体、字号、颜色）：经典白、网易云红、淡粉 / 绿 / 蓝、薄荷、薰衣草
- 可调字体、字号、当前行放大、下一行缩小、平滑滚动等

### 封面与播放页
- 播放页全屏专辑封面 + 磨砂玻璃效果
- 底部控制区**渐变融入背景**（参考网易云风格），无突兀白色底块
- 优先读取 MP3 内嵌封面，无封面时可联网获取（iTunes）

### 外观主题
- **应用主题**（整 App 配色与风格）：网易云、塞尔达、马里奥
- **浅色 / 深色 / 跟随系统**（网易云主题下生效）
- 歌词配色与应用主题独立，互不影响

### 车机操控
- **方向盘媒体键**：上一首 / 下一首 / 播放暂停
- **车内语音**：说「上一首」「下一首」等（需本 App 正在播放）
- 通知栏媒体控制

## 音乐与歌词放置

```
/sdcard/Music/
  ├── 歌手A/
  │   ├── 歌曲1.mp3
  │   └── 歌曲1.lrc
  └── 歌曲2.mp3
  └── 歌曲2.lrc
```

也可在 **设置 → 选择文件夹** 添加任意目录。

**没有本地 LRC 时：** 开启「联网获取歌词」，App 会自动从多个歌词源搜索。

**歌词不对时：** 在播放页 **长按歌词区域**，修改歌名/歌手后点「搜索」重新下载。

## 编译 APK

### GitHub Actions（推荐）

1. 推送代码到 GitHub（注意文件放在 `app/` 目录下，不要覆盖仓库根目录的 `build.gradle.kts`）
2. **Actions** 页手动 **Run workflow**
3. 下载 `app-debug.apk`

### Android Studio 本地编译

`app/build/outputs/apk/debug/app-debug.apk`

## 安装到车机

U 盘安装或 `adb install -r app-debug.apk`

## 车机必设权限（DiLink 4.0）

1. **存储权限**：允许读取音频
2. **显示在其他应用上层**：悬浮歌词
3. **极速模式白名单**：防后台被杀
4. **开机自启动**（如车机有该设置）：允许本应用开机启动
5. 开启 **开机自动启动并续播** + **启动时自动恢复播放**

## 使用步骤

1. **设置** 中添加扫描目录 → **扫描音乐**（首次扫描后列表会自动缓存）
2. **播放列表** 可按歌手浏览，或按时长排序
3. 点歌播放，长按歌词可重新搜索
4. 在 **设置 → 外观** 选择应用主题；在 **歌词配色** 调整歌词样式
5. 开启悬浮歌词 / 仪表屏歌词（仪表屏需车机支持副屏显示）

## 版本记录

| 版本 | 说明 |
|------|------|
| v3.2.0 | 多源联网歌词、播放列表缓存、播放页控制区渐变 UI、应用主题与歌词配色分离 |
| v3.1.0 | 开机自启动、歌手筛选、时长排序、歌词重搜、仪表屏歌词 |
| v3.0.1 | 方向盘 / 语音 / 通知栏媒体键切歌 |
| v3.0.0 | 全屏封面播放页、歌词主题、性能优化 |
| v2.1.0 | 文件夹选择、联网歌词与封面 |
